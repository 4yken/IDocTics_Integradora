import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.KeySoft.myapp',
  appName: 'IDocTics',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
